//float3 sdPos = objectPos;

/*
    float displace = sphereCentre + (sin(rayOrigin.x*sin(time)/3) +
                                 sin(rayOrigin.y*sin(time)/3) +
                                 sin(rayOrigin.z*sin(time)/3));

    float d1 = torus.sdTorus(rayOrigin, sphereRadius, 25);
    float d2 = box.sdBox(sphereCentre, float3(50, 50, 5));
    float td = d1;
    */

    /*
struct sdCalcs
{
    float3 calcNormal(float3 &p)
    {
        const float h = 0.0001;
        float3 n = float3(0.0);

        for(int i = 0, i < 4; i++)
        {
            float3 e = 0.5773 * (2.0 * float3())
        }
    }
};
*/